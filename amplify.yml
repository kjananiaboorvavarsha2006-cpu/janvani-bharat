version: 1
frontend:
  phases:
    preBuild:
      commands:
        - nvm use 18 || nvm install 18
        - node --version
        - cd frontend
        - npm install
    build:
      commands:
        - nvm use 18
        - cd frontend
        - npm run build
  artifacts:
    baseDirectory: frontend/dist
    files:
      - '**/*'
  cache:
    paths:
      - frontend/node_modules/**/*
